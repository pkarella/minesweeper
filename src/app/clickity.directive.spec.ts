import { ClickityDirective } from './clickity.directive';

describe('ClickityDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickityDirective();
    expect(directive).toBeTruthy();
  });
});
